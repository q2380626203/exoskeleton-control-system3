# 速度跟随模式状态机逻辑分析

基于代码分析，这是一个双电机协作的速度跟随控制系统。以下是详细的状态机逻辑：

## 状态定义（7个状态）

1. **SPEED_FOLLOW_IDLE** - 空闲周期
2. **SPEED_FOLLOW_WAITING** - 等待检测状态（ch6/ch7触发后）
3. **SPEED_FOLLOW_BUTTON_WAITING** - 按键触发等待状态
4. **SPEED_FOLLOW_MOTOR1_WORKING** - 电机1工作状态
5. **SPEED_FOLLOW_MOTOR2_WORKING** - 电机2工作状态
6. **SPEED_FOLLOW_PHASE1** - 抬腿阶段
7. **SPEED_FOLLOW_PHASE2** - 压腿阶段

## 触发模式对比

### 模式1：缓存区触发（ch6/ch7）
- **触发条件**: ch6_max 或 ch7_max > 阈值（默认10.0）
- **等待时间**: WAITING状态等待300ms
- **检测逻辑**:
  - ch6触发 → 等待300ms → 检测2号电机-v
  - ch7触发 → 等待300ms → 检测1号电机+v
- **超时保护**: WORKING状态超时1.2s
- **语音反馈**: 触发时播放"助力开启"，超时静止播放"助力关闭"

### 模式2：按键触发
- **触发条件**: 手动按键调用`startButtonWaiting()`
- **等待时间**: 无等待，立即进入BUTTON_WAITING
- **检测逻辑**: 同时检测两个电机，谁先触发就进入谁的PHASE1
  - 1号电机速度 > +2.0 rad/s → 1号抬腿
  - 2号电机速度 < -2.0 rad/s → 2号抬腿
- **超时保护**: BUTTON_WAITING超时4s
- **语音反馈**: 超时静止播放"助力关闭"

## 状态机流程图

```
                        ┌─────────────────────┐
                        │   初始状态: IDLE    │
                        └──────────┬──────────┘
                                   │
                ┌──────────────────┴──────────────────┐
                │                                     │
         缓存区触发                              按键触发
      (ch6/ch7超阈值)                        (startButtonWaiting)
                │                                     │
                ↓                                     ↓
        ┌───────────────┐                    ┌───────────────────┐
        │   WAITING     │                    │  BUTTON_WAITING   │
        │  等待300ms    │                    │  同时检测两电机   │
        └───────┬───────┘                    │  超时4s           │
                │                            └─────────┬─────────┘
     ┌──────────┴──────────┐                          │
     │                     │                ┌─────────┴─────────┐
  ch6触发              ch7触发               │                  │
  检测2号-v            检测1号+v        1号速度>+2.0      2号速度<-2.0
     │                     │                │                  │
     └──────────┬──────────┘                └─────────┬────────┘
                ↓                                     ↓
        ┌───────────────┐                    ┌───────────────┐
        │   PHASE1      │                    │   PHASE1      │
        │  抬腿阶段     │◄───────────────────┤  抬腿阶段     │
        │  超时500ms    │                    │  超时500ms    │
        │ 速度反转提前  │                    │ 速度反转提前  │
        └───────┬───────┘                    └───────┬───────┘
                │                                     │
                ↓                                     ↓
        ┌───────────────┐                    ┌───────────────┐
        │   PHASE2      │                    │   PHASE2      │
        │  压腿阶段     │                    │  压腿阶段     │
        │  超时350ms    │                    │  超时350ms    │
        │ 速度归零提前  │                    │ 速度归零提前  │
        └───────┬───────┘                    └───────┬───────┘
                │                                     │
                └──────────────────┬──────────────────┘
                                   ↓
                        ┌─────────────────────┐
                        │       IDLE          │
                        │      100ms          │
                        └──────────┬──────────┘
                                   │
                ┌──────────────────┴──────────────────┐
                │                                     │
        电机1工作(active=1)              电机2工作(active=2)
                │                                     │
                ↓                                     ↓
    ┌───────────────────────┐          ┌───────────────────────┐
    │ MOTOR1_WORKING        │          │ MOTOR2_WORKING        │
    │ 检测+v触发(>2.0)      │          │ 检测-v触发(<-2.0)     │
    │ 超时: 按键4s/缓存1.2s │          │ 超时: 按键4s/缓存1.2s │
    └───────────┬───────────┘          └───────────┬───────────┘
                │                                   │
                ↓                                   ↓
        ┌───────────────┐                  ┌───────────────┐
        │    PHASE1     │                  │    PHASE1     │
        │  电机1抬腿    │                  │  电机2抬腿    │
        └───────────────┘                  └───────────────┘
                │                                   │
                └───────────────┬───────────────────┘
                                ↓
                          (循环往复)
```

## 关键逻辑说明

### 1. 启动触发机制 (speed_follow_mode.cpp:272-309)

**缓存区触发条件**: ch6_max > threshold 或 ch7_max > threshold

**触发行为**:
- 首次触发记录触发通道（6或7）
- 进入 WAITING 状态，等待300ms
- 如果双通道同时触发，选择数值更大的一方
- 设置 `_is_buffer_triggered = true` 标志

**按键触发条件**: 调用 `startButtonWaiting()`

**触发行为**:
- 直接进入 BUTTON_WAITING 状态
- 设置 `_is_button_triggered = true` 标志
- 同时检测两个电机，无需等待

### 2. BUTTON_WAITING 状态 (speed_follow_mode.cpp:338-371)

**持续时间**: 无固定时间，立即检测

**检测逻辑**:
- 检测1号电机速度 > +2.0 → 捕获速度，1号开始抬腿，2号下个周期工作
- 检测2号电机速度 < -2.0 → 捕获速度，2号开始抬腿，1号下个周期工作

**电机控制**: 两个电机都保持空闲参数

**超时保护**: 无（在WORKING状态有4s超时）

### 3. WAITING 状态 (speed_follow_mode.cpp:373-424)

**持续时间**: 300ms（可配置）

**检测逻辑**:
- ch6触发: 等待300ms后检测电机2速度 < -2.0，触发则电机2开始抬腿
- ch7触发: 等待300ms后检测电机1速度 > +2.0，触发则电机1开始抬腿

**电机控制**: 两个电机都保持空闲参数

**速度捕获**: 检测到触发时捕获当前电机速度到 `_captured_velocity`

### 4. PHASE1 - 抬腿阶段 (speed_follow_mode.cpp:534-596)

**持续时间**: 500ms（可通过 `_phase1_timeout_ms` 动态调整）

**动态速度跟随**:
- 每个控制周期根据当前电机的实时速度 × 0.8（`_velocity_scale`）更新参数
- 电机1抬腿: vel = motor_data.vel × 0.8, torque = +0.7, kd = 0.05
- 电机2抬腿: vel = motor_data.vel × 0.8, torque = -0.7, kd = 0.05

**提前退出条件**（速度反转检测）:
- 电机1: 从+v变为-v
- 电机2: 从-v变为+v

**完成后**: 切换工作电机，进入PHASE2

### 5. PHASE2 - 压腿阶段 (speed_follow_mode.cpp:598-652)

**持续时间**: 350ms（可通过 `_phase2_timeout_ms` 动态调整）

**动态速度跟随**:
- 每个控制周期根据当前电机的实时速度 × 0.8 更新参数
- 电机1压腿: vel = motor_data.vel × 0.8, torque = -0.3, kd = 0.03
- 电机2压腿: vel = motor_data.vel × 0.8, torque = +0.3, kd = 0.03

**提前退出条件**（速度归零检测）:
- 电机速度降低到阈值范围内：-0.5 < vel < +0.5 (可通过 `_phase2_vel_threshold` 调整)

**完成后**: 进入IDLE

### 6. IDLE - 空闲周期 (speed_follow_mode.cpp:654-677)

**持续时间**: 100ms（可分别配置 `_config_motor1.idle_duration_ms` 和 `_config_motor2.idle_duration_ms`）

**电机控制**: 两个电机都保持零力矩状态（mode=1, vel=0, torque=0）

**完成后**: 根据 `_active_motor` 进入对应的WORKING状态

### 7. MOTOR1/MOTOR2_WORKING 状态 (speed_follow_mode.cpp:426-532)

**电机1工作**: 检测速度 > +2.0 rad/s，触发则电机1抬腿

**电机2工作**: 检测速度 < -2.0 rad/s，触发则电机2抬腿

**超时保护**:
- 按键触发模式: 4秒内未检测到速度触发 → 设置 `_is_stationary = true`，自动关闭速度跟随模式并清空缓存
- 缓存区触发模式: 1.2秒内未检测到速度触发 → 设置 `_is_stationary = true`，自动关闭速度跟随模式并清空缓存

**缓存清理**: 超时时会清空位置缓存区和ch6/ch7差值缓存区

## 动态参数系统

### 可调参数（speed_follow_mode.cpp:160-171）

| 参数名称 | 默认值 | 说明 |
|---------|-------|------|
| `_phase1_timeout_ms` | 500ms | PHASE1超时时间（抬腿阶段） |
| `_phase2_timeout_ms` | 350ms | PHASE2超时时间（压腿阶段） |
| `_velocity_scale` | 0.8 | 速度缩放因子（实时速度×该值） |
| `_phase2_vel_threshold` | ±0.5 rad/s | PHASE2完成速度阈值 |

### 速度捕获与跟随机制

**捕获时机**:
- WAITING状态检测到速度触发时捕获 `_captured_velocity`
- BUTTON_WAITING状态检测到速度触发时捕获 `_captured_velocity`
- WORKING状态检测到速度触发时捕获 `_captured_velocity`

**跟随策略**:
- PHASE1/PHASE2每个控制周期（2ms）都会根据当前电机的实时速度更新参数
- 更新公式: `scaled_vel = motor_data.vel * _velocity_scale`
- 只更新对应抬腿电机的参数（通过 `motor_data.id` 判断）

## 数据流（每2ms周期）

```
UART RX → unpack_motor_data_go_m8010_6() → MotorDataA1{id, pos, vel, t, temp, ...}
    ↓
position_buffer_add_motorX() → wave analysis → ch6/ch7_max
    ↓
speed_follow.update(motor_data_1) → 检查 motor_data.id == 1 → 更新电机1
    ↓
speed_follow.update(motor_data_2) → 检查 motor_data.id == 2 → 更新电机2
    ↓
checkThresholdAndActivate(ch6_max, ch7_max) → 如果超阈值 → 激活WAITING
    ↓
setMotorParams() → 修改 global_motor_1/2 (mutex) → 下个周期读取新参数
```

## 触发标志管理

### 标志位说明

| 标志位 | 用途 | 设置时机 | 清除时机 |
|-------|------|---------|---------|
| `_is_active` | 速度跟随是否激活 | 缓存区/按键触发时 | WORKING超时或手动关闭 |
| `_is_button_triggered` | 是否为按键触发 | 按键触发时 | WORKING超时 |
| `_is_buffer_triggered` | 是否为缓存区触发 | ch6/ch7触发时 | 外部调用`clearBufferTriggeredFlag()` |
| `_is_stationary` | 是否进入静止状态 | WORKING超时 | 外部调用`clearStationaryFlag()` |
| `_first_trigger_detected` | 首次触发检测 | ch6/ch7首次超阈值 | WORKING超时或手动关闭 |

### 语音播放逻辑

**助力开启语音**:
- 条件: `_is_buffer_triggered == true`
- 播放后调用: `speed_follow.clearBufferTriggeredFlag()`

**助力关闭语音**:
- 条件: `_is_stationary == true` 且（`_is_button_triggered == true` 或 `_is_buffer_triggered` 曾为true）
- 播放后调用: `speed_follow.clearStationaryFlag()`

## 重要实现细节

### 1. 双电机同步控制

- `update()` 函数在每个控制周期被调用**两次**（motor_data_1 和 motor_data_2）
- 必须通过 `motor_data.id` 判断当前处理的是哪个电机的数据
- 动态参数更新只影响对应的抬腿电机

### 2. 互斥锁保护

- 所有访问 `global_motor_1` 和 `global_motor_2` 的操作都使用 `motor_params_mutex`
- 读操作在 `motor_control_task()` 发送命令前
- 写操作在 `speed_follow.setMotorParams()` 中

### 3. 提前退出机制

**PHASE1提前退出**:
- 检测速度反转（+v→-v 或 -v→+v）
- 避免抬腿动作持续过长

**PHASE2提前退出**:
- 检测速度降低到 ±0.5 rad/s 范围内
- 表示压腿动作已完成，电机速度接近零

### 4. 超时保护

- BUTTON_WAITING → WORKING: 超时4s
- WAITING → WORKING: 超时1.2s
- PHASE1: 超时500ms（可调）
- PHASE2: 超时350ms（可调）

## 配置参数（speed_follow_mode.cpp:90-172）

### 电机1配置

| 参数 | 值 | 说明 |
|-----|---|------|
| trigger_speed | +2.0 rad/s | 触发速度阈值 |
| phase1_duration_ms | 500ms | PHASE1持续时间 |
| phase2_duration_ms | 350ms | PHASE2持续时间 |
| waiting_duration_ms | 300ms | 等待时间 |
| idle_duration_ms | 100ms | 空闲时间 |
| phase1: vel | +10.0 rad/s | 抬腿速度（初始值） |
| phase1: torque | +0.7 N·m | 抬腿力矩 |
| phase1: kd | 0.05 | 速度增益 |
| phase2: vel | -10.0 rad/s | 压腿速度（初始值） |
| phase2: torque | -0.3 N·m | 压腿力矩 |
| phase2: kd | 0.03 | 速度增益 |

### 电机2配置

| 参数 | 值 | 说明 |
|-----|---|------|
| trigger_speed | -2.0 rad/s | 触发速度阈值 |
| phase1_duration_ms | 500ms | PHASE1持续时间 |
| phase2_duration_ms | 350ms | PHASE2持续时间 |
| waiting_duration_ms | 300ms | 等待时间 |
| idle_duration_ms | 100ms | 空闲时间 |
| phase1: vel | -10.0 rad/s | 抬腿速度（初始值） |
| phase1: torque | -0.7 N·m | 抬腿力矩 |
| phase1: kd | 0.05 | 速度增益 |
| phase2: vel | +10.0 rad/s | 压腿速度（初始值） |
| phase2: torque | +0.3 N·m | 压腿力矩 |
| phase2: kd | 0.03 | 速度增益 |

注意: PHASE1/PHASE2的vel值会被实时速度×0.8动态替换，上述vel值仅为参考基准值。

## 常见问题

### 1. 为什么PHASE1/PHASE2的vel参数设置为±10但实际不使用？

- 这些是初始参考值，实际运行时会被 `motor_data.vel * _velocity_scale` 动态替换
- 动态跟随机制每2ms更新一次，确保电机速度紧跟实际运动状态

### 2. 如何调整抬腿/压腿的力度？

- 修改 `phase1.torque` 和 `phase2.torque` 参数
- 可通过Web接口实时调整

### 3. 如何优化步态循环频率？

- 调整 `idle_duration_ms`（空闲时间）
- 调整 `_phase1_timeout_ms` 和 `_phase2_timeout_ms`（阶段超时）
- 调整 `_phase2_vel_threshold`（压腿完成阈值，降低该值会提前完成）

### 4. 按键触发和缓存区触发有什么区别？

- **按键触发**: 超时4s，立即检测两电机，谁先触发就进入谁的PHASE1，无需等待
- **缓存区触发**: 超时1.2s，先判断ch6/ch7，等待300ms后检测对应电机，更精准但有延迟
